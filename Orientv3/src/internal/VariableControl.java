package internal;

public class VariableControl {
	/*
	 * will use text files for this
	 * format: 
	 * name.*
	 * name = variable name
	 * * = variable type (file type: image and string currently)
	 * 
	 */
}
